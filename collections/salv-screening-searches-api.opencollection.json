{
  "opencollection": "1.0.0",
  "info": {
    "name": "Salv AML alert screening-searches API",
    "version": "1.0.9"
  },
  "request": {
    "auth": {
      "type": "oauth2",
      "flow": "client_credentials",
      "accessTokenUrl": "https://app.salv.com/oauth/token",
      "credentials": {
        "clientId": "{{clientId}}",
        "clientSecret": "{{clientSecret}}"
      }
    }
  },
  "items": [
    {
      "info": {
        "name": "screening-searches",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Run screening search",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://{environment}.salv.com/api/v2/screening-searches",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Checks if provided name matches a sanctioned person or entity."
        }
      ]
    }
  ],
  "bundled": true
}